import 'package:flutter/material.dart';

const kSecondaryColor = Color(0xFFFE6D8E);
const kTextColor = Color(0xFF12153d);
const kTextLightColor = Color(0xFF9a9bb2);
const kFillStartColor = Color(0xFFfcc419);
const kEnabledColor = Colors.black54;

const kDefaultPadding = 20.0;

const TextStyle kLabelText = TextStyle(
    color: kTextLightColor, fontSize: 17, fontWeight: FontWeight.bold);

const kDefaultShadow =
    BoxShadow(offset: Offset(0, 4), blurRadius: 4, color: Colors.black26);
